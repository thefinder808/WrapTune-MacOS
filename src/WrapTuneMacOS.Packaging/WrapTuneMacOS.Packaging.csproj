<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Tests exercise the internal CFBF/MSI parsers directly. -->
    <InternalsVisibleTo Include="WrapTuneMacOS.Packaging.Tests" />
  </ItemGroup>

</Project>
